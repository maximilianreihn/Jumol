using Test

function strategies_module_tests(exclude_time_intensiv)
    @test 1==1
    """
    No tests in this file since every other file uses the structure package, meaning if things are changed most other tests will
    fail, all relevant functions are used in other tests. If that changes, added tests would be appreciated.
    """
end

